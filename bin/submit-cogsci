#!/usr/bin/env bash
set -x

title="EdmistonDerexLupyan-IteratedProblemSolving$1"
function rename {
    basename=$(basename "$1")
    ext=${basename##*.}
    cp $1 ${title}.${ext}
}
rename docs/cogsci.Rmd
rename docs/cogsci.pdf
rename docs/cogsci.docx

zip -j cogsci$1 ${title}*
rm ${title}*
